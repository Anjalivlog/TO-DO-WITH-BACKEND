<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Shadows+Into+Light&display=swap" rel="stylesheet">
    <!-- font awesome icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" integrity="sha512-KfkfwYDsLkIlwQp6LFnl8zNdLGxu9YAA1QvwINks4PhcElQSvqcyVLLD9aMhXd13uQjoXtEKNosOWaZqXgel0g==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap');

        * {
            font-family: inherit;
            padding: 0px;
            margin: 0px;
            box-sizing: border-box;
        }

        /* :root {
            --linear-grad: linear-gradient(to right, #141E30, #243B55);
            --grad-clr1: #141E30;
            --grad-clr2: #243B55;
        } */

        body {
            height: 100vh;
            /* background: #f6f5f7; */

            /* background: #cfd8dc; */

            background-color: #a0a8a0;
            display: grid;
            place-content: center;
        }

        .container {
            background: white;
            border-radius: 10px;
            box-shadow: 0 14px 28px rgba(0, 0, 0, 0.25), 0 10px 10px rgba(0, 0, 0, 0.22);
            position: relative;
            overflow: hidden;
            width: 768px;
            max-width: 100%;
            min-height: 480px;
        }

        .form-container {
            /* margin: 0 40px; */
            /* padding: 50px 30px; */
            /* position: absolute; */
            margin-left: 320px;
            padding: 50px 30px;
            top: 0;
            height: 100%;
        }

        .sign-in-container { 
            width: 50%;
            margin-top: 30px;
        }

        .subheading {
            display: flex;
            justify-content: center;
            font-size: small;
        }

        .overlay-container {
            position: absolute;
            top: 0;
            /* left: 50%; */
            width: 40%;
            height: 100%;
            overflow: hidden;
        }

        .overlay {
            /* background: var(--linear-grad); */
            background: -webkit-radial-gradient(right, rgba(48, 64, 64, 1.0) 0%, rgba(0, 0, 0, 1.0) 170%);
            background: -moz-radial-gradient(right, rgba(48, 64, 64, 1.0) 0%, rgba(0, 0, 0, 1.0) 170%);
            background: radial-gradient(ellipse at right, rgba(48, 64, 64, 1.0) 0%, rgba(0, 0, 0, 1.0) 170%);
            background-repeat: no-repeat;
            background-size: cover;
            background-position: 0 0;
            color: white;
            position: relative;
            left: -100%;
            height: 100%;
            width: 200%;
        }

        .overlay-panel {
            position: absolute;
            display: block;
            /* align-items: center; */
            justify-content: center;
            text-align: center;
            top: 0;
            height: 100%;
            width: 50%;
            padding: 25% 15px;
        }

        .overlay-left {
            left: 50%;
            /* transform: translateX(-20%); */
        }

        .social-container {
            display: flex;
            justify-content: center;
            margin: 10px 0;
        }

        .social-container a:hover {
            background: var(--grad-clr2);
            color: white;
        }

        .social-container a {
            border: 1px solid #dddddd;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin: 0 5px;
            height: 40px;
            width: 40px;
            text-decoration: none;
        }

        .infield {
            position: relative;
            margin: 20px 0;
        }

        .infield input {
            background: #f6f5f7;
            border: none;
            padding: 12px 15px;
            width: 100%;
        }

        .infield label {
            position: absolute;
            left: 15px;
            top: 12px;
            pointer-events: none;
            transition: 0.2s ease all;
        }

        .loginhead {
            margin-top: 0;
            margin-bottom: 20px;
        }
        
        .button2 {
            border: 2px solid white;
            margin-top: 20px;
        }

        button {
            border-radius: 20px;
            border: none;
            /* background: var(--grad-clr1);
            color: white; */
            color: #04202c;
            background-color: #c9d1c8;
            font-weight: bold;
            padding: 12px 30px;
            margin-top: 0px;
            cursor: pointer;
        }

        button:hover {
            /* background: var(--grad-clr2); */
            background-color: #a0a8a0;
            border: none;
        }
    </style>
</head>
<body>
    <div class="container" id="container">
        <div class="form-container sign-in-container">
            <!-- <form action="/login" method="POST"> -->
                <center><h1>Sign in</h1></center>
                <div class="social-container">
                    <a href="#" class="social"><i class="fab fa-facebook-f"></i></a>
                    <a href="#" class="social"><i class="fab fa-google-plus-g"></i></a>
                    <a href="#" class="social"><i class="fab fa-linkedin-in"></i></a>
                </div>
                <span class="subheading">or use your account</span>
                <div class="infield">
                    <input type="text" id="login_field" class="submit_field" placeholder="Email" name="loginId" required/>
                    <label></label>
                </div>
                <div class="infield">
                    <input type="password" id="password_field" class="submit_field" placeholder="Password" name="password" required/>
                    <label></label>
                </div>
                <a href="#" class="forgot">Forgot your password?</a>
                <button type="submit" class="login_form">Sign In</button>
            <!-- </form> -->
        </div>
        <div class="overlay-container">
            <div class="overlay">
                <div class="overlay-panel overlay-left">
                    <h1 class="loginhead">Welcome Back!</h1>
                    <p>To keep connected with us please login with your personal info</p>
                    <a><button class="button2">Sign Up</button></a>
                </div>
            </div>
        </div>
    </div>


    
    <script src="https://unpkg.com/axios@1.6.7/dist/axios.min.js"></script>
    <script src="/browsertow.js"></script>

</body>
</html>
